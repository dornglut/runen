# Core Borrowing

Status: **provisional normative; incomplete**

Shared and exclusive borrowing are distinct semantic concepts.

A borrow grants access according to its borrowing contract without transferring the underlying value by move.

Illustrative spellings such as `&T` and `&mut T` do not freeze grammar.

The complete lifetime, reborrow, aliasing, and interior-mutability rules are not defined by this revision.