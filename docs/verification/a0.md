# A0 Verification Contract

Status: **non-normative conformance-oracle documentation**

This document owns the test obligations for the executable A0 oracle. The normative semantics exercised by A0 are owned by `spec/language/core/value-storage.md`.

## Core MIR validation boundary

Raw `runen-core-ir::Body` is a construction/data form. It is not executable by the reference machine until `validate_body` establishes the structural and language-validity rules represented by the current A0 MIR and returns `ValidatedBody`. This is language validation, not the environment-admission phase defined by `spec/language/lifecycle.md`.

Validation rejects unknown identifiers and types, recursive by-value type graphs unsupported by the current representation, invalid block targets and place projections, operand/value type mismatch, `Copy` of a non-copy type, and `Assign` through an immutable local.

Validation also proves the current MIR's reachable initialization-state transitions. `Init` requires Never-initialized storage; reads, moves, and copies require fully initialized storage; explicit `Drop` requires at least one Live subobject. Because the current A0 control-flow form has one successor per nonterminal block, validation follows the exact abstract storage-state path and terminates successfully when a block/storage-state pair repeats.

The reference machine accepts only `ValidatedBody`. Its execution API therefore has no malformed-MIR or language-invalid-MIR result. A terminating execution produces `ExecutionReport`; cyclic valid MIR may diverge. A represented `Terminator::Fault` is a defined terminal execution, not a validation error, and performs the cleanup required by the normative value/storage semantics.

## Required cases

The A0 suite covers at least:

- move invalidates the source, with a later use rejected by MIR validation;
- copy preserves the source;
- partial move preserves disjoint live sub-places and makes the aggregate unreadable as a whole;
- independently initialized fields can form a fully initialized aggregate;
- non-copy values cannot be copied;
- `Init` cannot reinitialize Dead storage;
- mutable `Assign` can initialize Never-initialized storage;
- mutable `Assign` can reinitialize Dead storage;
- mutable `Assign` can replace partially initialized aggregate storage while destroying only live old subobjects;
- immutable assignment is rejected;
- assignment destroys a live replacement target before writing the new value;
- explicit partial-aggregate drop destroys only live subobjects;
- moved or already-dropped values are not destroyed twice;
- field and local cleanup order matches the normative value/storage semantics;
- defined fault cleanup destroys each live value exactly once;
- state-changing loops are checked across repeated iterations, while a repeated valid block/storage-state pair is recognized as possible divergence rather than a validation failure.

## Semantic and verification-only constructs

`Statement::Read` represents the normative non-consuming Core read operation. The current A0 proving MIR discards the read value because later computation semantics are outside this subset. Readability and ownership behavior are semantic; recording that read in the oracle is not.

`ScalarType::TrackedFixture` and `Value::TrackedFixture` are synthetic non-copy fixture values with observable destruction solely to make ownership and destruction behavior testable. They are not Runen language scalar or value primitives.

`runen-reference::VerificationEvent`, `VerificationWriteKind`, and `ExecutionReport::verification_events` are verification instrumentation. Read, move, copy, write, and fixture-drop records expose the reference machine's internal semantic transitions to tests; they are not the observable program trace defined by `spec/language/behavior.md`.

## Oracle independence

The oracle must model Runen state explicitly. Host ownership, host destruction, native addresses, allocator identity, container order, and host scheduling are not semantic oracles.
